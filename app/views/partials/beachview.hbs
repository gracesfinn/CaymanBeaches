<section class="ui raised segment">
    <div class="ui grid">
        <aside class="six wide column">
            <h1 class="ui header">{{beach.name}}</h1>
            <img src="{{beach.imageMain}}" class="ui large rounded image">
        </aside>
        <article class="eight wide column">
            <table class="ui celled table segment">
                <thead>
                <tr>
                    <th>Name</th>
                    <th>Description</th>
                    <th>Category</th>
                </tr>
                </thead>
                <tbody>

                    <tr>
                        <td> {{beach.name}} </td>
                        <td> {{beach.description}} </td>
                        <td> {{beach.categories}} </td>

                    </tr>


                </tbody>
            </table>
        </article>
    </div>
</section>